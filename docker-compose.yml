services:
  agent-console:
    build:
      context: .
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.12-slim}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
        PIP_EXTRA_INDEX_URL: ${PIP_EXTRA_INDEX_URL:-}
        PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST:-}
        PIP_DEFAULT_TIMEOUT: ${PIP_DEFAULT_TIMEOUT:-180}
        PIP_RETRIES: ${PIP_RETRIES:-10}
        REQUIREMENTS_FILE: ${REQUIREMENTS_FILE:-requirements-deploy.txt}
        INSTALL_RAG_DEPS: ${INSTALL_RAG_DEPS:-0}
    container_name: agent-console
    restart: unless-stopped
    env_file:
      - ${AGENT_CONSOLE_ENV_FILE:-.env}
    environment:
      RAG_ENABLED: ${RAG_ENABLED:-0}
      HISTORY_VECTOR_ENABLED: ${HISTORY_VECTOR_ENABLED:-0}
      SECURITY_RAG_AUTO_CONTEXT_ENABLED: ${SECURITY_RAG_AUTO_CONTEXT_ENABLED:-0}
      SECURITY_RAG_PLUGIN_ENABLED: ${SECURITY_RAG_PLUGIN_ENABLED:-0}
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./storage:/app/storage
      - ./memory:/app/memory
      - ./.sessions:/app/.sessions
      - ./.task_sessions:/app/.task_sessions
      - ./.task_sandbox:/app/.task_sandbox
      - ./.tasks:/app/.tasks
      - ./.team:/app/.team
      - ./.transcripts:/app/.transcripts
      - ./.users:/app/.users

  telegram-worker:
    profiles:
      - telegram
    build:
      context: .
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.12-slim}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
        PIP_EXTRA_INDEX_URL: ${PIP_EXTRA_INDEX_URL:-}
        PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST:-}
        PIP_DEFAULT_TIMEOUT: ${PIP_DEFAULT_TIMEOUT:-180}
        PIP_RETRIES: ${PIP_RETRIES:-10}
        REQUIREMENTS_FILE: ${REQUIREMENTS_FILE:-requirements-deploy.txt}
        INSTALL_RAG_DEPS: ${INSTALL_RAG_DEPS:-0}
    container_name: taleclaw-telegram
    restart: unless-stopped
    env_file:
      - ${AGENT_CONSOLE_ENV_FILE:-.env}
    environment:
      RAG_ENABLED: ${RAG_ENABLED:-0}
      HISTORY_VECTOR_ENABLED: ${HISTORY_VECTOR_ENABLED:-0}
      SECURITY_RAG_AUTO_CONTEXT_ENABLED: ${SECURITY_RAG_AUTO_CONTEXT_ENABLED:-0}
      SECURITY_RAG_PLUGIN_ENABLED: ${SECURITY_RAG_PLUGIN_ENABLED:-0}
    command: ["python", "telegram_worker.py"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./storage:/app/storage
      - ./memory:/app/memory
      - ./.sessions:/app/.sessions
      - ./.task_sessions:/app/.task_sessions
      - ./.task_sandbox:/app/.task_sandbox
      - ./.tasks:/app/.tasks
      - ./.team:/app/.team
      - ./.transcripts:/app/.transcripts
      - ./.gateway:/app/.gateway
      - ./.users:/app/.users

  feishu-worker:
    profiles:
      - feishu
    build:
      context: .
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.12-slim}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
        PIP_EXTRA_INDEX_URL: ${PIP_EXTRA_INDEX_URL:-}
        PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST:-}
        PIP_DEFAULT_TIMEOUT: ${PIP_DEFAULT_TIMEOUT:-180}
        PIP_RETRIES: ${PIP_RETRIES:-10}
        REQUIREMENTS_FILE: ${REQUIREMENTS_FILE:-requirements-deploy.txt}
        INSTALL_RAG_DEPS: ${INSTALL_RAG_DEPS:-0}
    container_name: taleclaw-feishu
    restart: unless-stopped
    env_file:
      - ${AGENT_CONSOLE_ENV_FILE:-.env}
    environment:
      RAG_ENABLED: ${RAG_ENABLED:-0}
      HISTORY_VECTOR_ENABLED: ${HISTORY_VECTOR_ENABLED:-0}
      SECURITY_RAG_AUTO_CONTEXT_ENABLED: ${SECURITY_RAG_AUTO_CONTEXT_ENABLED:-0}
      SECURITY_RAG_PLUGIN_ENABLED: ${SECURITY_RAG_PLUGIN_ENABLED:-0}
    command: ["python", "feishu_worker.py"]
    ports:
      - "127.0.0.1:8010:8010"
    volumes:
      - ./storage:/app/storage
      - ./memory:/app/memory
      - ./.sessions:/app/.sessions
      - ./.task_sessions:/app/.task_sessions
      - ./.task_sandbox:/app/.task_sandbox
      - ./.tasks:/app/.tasks
      - ./.team:/app/.team
      - ./.transcripts:/app/.transcripts
      - ./.gateway:/app/.gateway
      - ./.users:/app/.users


  qdrant:
    profiles:
      - rag
    image: qdrant/qdrant:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:6333:6333"
      - "127.0.0.1:6334:6334"
    volumes:
      - ./qdrant_storage:/qdrant/storage

  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-agent_console}
      POSTGRES_USER: ${POSTGRES_USER:-agent}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-agent_dev_password}
    ports:
      - "127.0.0.1:${POSTGRES_HOST_PORT:-55432}:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
