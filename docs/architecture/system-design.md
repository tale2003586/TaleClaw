# Runtime System Design

The authoritative runtime design is documented in
[`runtime-system-design.md`](runtime-system-design.md). It covers the final
TurnCoordinator -> Runtime -> AgentRunner -> ReasoningLoop chain, state
ownership, ContextSnapshot transactions, bounded recovery, ToolSpec metadata,
Thinking capability mapping and future Sandbox/TaskPlan extension boundaries.
