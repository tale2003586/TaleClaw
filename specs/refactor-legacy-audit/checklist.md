# 生产代码重构遗留审计与修复 Checklist

> 每一项必须用实际命令、数据库查询或行为测试验证；完成开发后记录结果。

## 实现完整性

- [x] C1：全部生产兼容与旧命名候选已分类。（验证：审计矩阵每个扫描命中均有 category、evidence、action）
- [x] C2：八个 PostgreSQL 相关模块均有结构契约核对。（验证：报告逐项列出建表、升级、读取、写入、约束和索引）
- [x] C3：公共字段盘点只读取当前 schema。（验证：临时 search path 与 public 同名表隔离测试通过）
- [x] C4：会话旧结构能完整迁移到 `active_agent`。（验证：旧库与半迁移库测试后旧列不存在、数据值正确）
- [x] C5：Telegram 旧 outbox 可升级。（验证：旧文本消息保留，新文档消息可入队）
- [x] C6：飞书旧 outbox 可升级。（验证：旧文本消息保留，新文档消息可入队）
- [x] C7：Web 鉴权、记忆归档、trace 和工具结果均有闭环结论。（验证：专项测试及报告证据）
- [x] C8：非数据库兼容入口均有保留或移除依据。（验证：调用方扫描与行为测试）

## 数据迁移安全

- [x] C9：每个删除旧列的迁移都先验证目标字段有效。（验证：迁移测试含空值/半迁移数据场景）
- [x] C10：迁移失败不会提交半迁移结构。（验证：故意构造不可承接数据，事务回滚后结构和行数不变）
- [x] C11：迁移可重复执行。（验证：同一 store 连续初始化两次，结构与行数不变）
- [x] C12：迁移不逐行搬运业务数据。（验证：实现使用集合式 SQL，测试数据量不影响语义）
- [x] C13：外键、唯一约束和必要索引保持有效。（验证：PostgreSQL catalog 查询与约束行为测试）

## 当前实例

- [x] C14：迁移前结构与行数摘要已记录，且不含业务正文。（验证：`audit-report.md` 快照）
- [x] C15：实际执行的每项迁移已有临时 schema 测试覆盖。（验证：报告中迁移与测试一一映射）
- [x] C16：迁移后必填字段无 NULL/无效空字符串。（验证：各目标列聚合查询结果为 0）
- [x] C17：迁移前后业务行数符合预期，没有业务记录丢失。（验证：前后快照对比）
- [x] C18：全部实际 store 可初始化。（验证：使用当前 DSN 依次构造并关闭 store）
- [x] C19：隔离读写验证完成且测试记录已清理。（验证：写入、读取、删除后行数回到基线）

## 兼容与范围

- [x] C20：不同领域的合法 `mode` 未被机械改名。（验证：trace、检索、工具策略相关测试通过）
- [x] C21：协议或公开入口兼容层未被无依据移除。（验证：子代理、网关、工具 schema、AgentSpec 测试通过）
- [x] C22：历史报告、评测脚本和 benchmark fixtures 未被修改。（验证：文件改动清单）
- [x] C23：未修改模型、密钥、登录账号和网络配置。（验证：配置文件改动与敏感模式扫描）

## 编译与测试

- [x] C24：修改过的 Python 文件可编译。（验证：`python -m compileall` 针对修改模块成功）
- [x] C25：所有 PostgreSQL store 专项测试通过。（验证：对应 `python -m pytest -q` 命令退出码为 0）
- [x] C26：所有兼容入口专项测试通过。（验证：对应测试命令退出码为 0）
- [x] C27：全量测试无本次引入的失败。（验证：`python -m pytest -q`，记录 passed/skipped/failed）
- [x] C28：无密钥、密码或用户正文进入审计报告。（验证：敏感模式扫描与人工查看通过）

## 端到端场景

- [x] C29：旧会话库升级后，用户发送消息可创建或更新 session，不再出现旧字段非空约束异常。（验证：旧结构临时 schema + SessionManager 完整保存/加载）
- [x] C30：旧网关 outbox 升级后，已有文本消息继续发送，新文档消息可以排队。（验证：Telegram 与飞书 store 完整行为测试）
- [x] C31：已迁移数据库重启所有 store 后结构不再变化，已有数据仍可读取。（验证：两次快照相同并完成只读加载）
- [x] C32：当前 Web 所依赖的鉴权和 session store 同时初始化成功。（验证：当前 DSN 初始化两个 store，无异常退出）

## 实际验收结果

- 32/32 条检查通过。
- PostgreSQL store 专项：76 passed。
- 生产兼容入口专项：53 passed。
- Session 失败回滚与迁移专项：6 passed。
- 全量回归：488 passed，2 条第三方弃用 warning。
- 当前实例迁移前后 13 张表行数逐表一致，隔离验证记录已清理。
- 普通测试默认关闭共享 trace index；最终全量回归后 trace 行数仍为 1,755 / 20,753。
- 临时 PostgreSQL schema 残留数为 0，真实 Python 源码编译通过。
- 完整证据见 `audit-report.md`。
